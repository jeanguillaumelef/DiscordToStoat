# Discord bot token — Discord Developer Portal → Applications → (your app) → Bot → Token
DISCORD_TOKEN=

# Stoat bot token — User Settings → My Bots → (your bot) → Token
STOAT_TOKEN=

# Optional: override the Stoat API base URL (defaults to https://stoat.chat/api)
# STOAT_BASE_URL=
